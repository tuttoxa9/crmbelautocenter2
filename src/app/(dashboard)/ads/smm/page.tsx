import React from "react";
import SmmUploadClient from "./SmmUploadClient";

export const metadata = {
  title: "Загрузка видео (SMM) | Bel Auto Center",
};

export default function SmmUploadPage() {
  return <SmmUploadClient />;
}
